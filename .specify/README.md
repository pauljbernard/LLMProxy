# SpecKit-Inspired Structure

This repository uses a SpecKit-inspired structure with a custom SpecKit-aligned specification set:

- `.specify/memory/constitution.md` for the project constitution
- `specs/001-llmproxy-foundation/` for the primary specification, plan, and task flow

The `docs/specs/` directory remains the extended reference library that elaborates the core specification artifacts.
