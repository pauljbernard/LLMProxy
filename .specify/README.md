# SpecKit Alignment

This repository uses a SpecKit-aligned structure:

- `.specify/memory/constitution.md` for the project constitution
- `specs/001-llmproxy-foundation/` for the primary specification, plan, and task flow

The `docs/specs/` directory remains the extended reference library that elaborates the core SpecKit artifacts.
